import 'package:flutter/material.dart';

class SegundoSqliteScreen extends StatelessWidget {
  static const String name = "Segundo_SQLite_Screen";
  const SegundoSqliteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("segundo ensayo de sqlite"),
      ),
      body: const Placeholder(),
    );
  }
}
